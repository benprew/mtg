- @title = "Homepage"
%div{ :style => "padding: 1em" }
  Welcome to the MTG pricing system.  You can search using the search box in the header, or look at the list of cards below.

  %h3 What's New
  %ul
    %li SETS and CARDS menus (in the upper-right corner)
    %li
      see
      %a{ :href=>'/updates' } Updates
      for a complete list


%div{ :style => "padding-left: 1em; padding-right: 1em" }
  %div{ :style => "float: left; width: 49%" }
    = render_dataset("Interesting Auctions", @interesting_auctions)
    = render_dataset("Most Auctioned Cards", @highest_volume_cards)

  %div{ :style => "float: right; width: 49%" }
    = render_dataset("Most Expensive Cards", @most_expensive_cards)
    = render_dataset("Most Expensive Alara Reborn Cards", @most_expensive_alara_reborn_cards)
